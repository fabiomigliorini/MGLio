<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mgpapelaria">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TransacaoJsonActivity"></activity>
        <activity android:name=".activity.TransacaoActivity" />
        <activity android:name=".activity.ListaTransacoesActivity" />
        <activity android:name=".activity.TesteCoresActivity" />
        <activity android:name=".activity.OperacaoCanceladaActivity" />
        <activity android:name=".activity.PagamentoActivity" />
        <activity android:name=".activity.ListaVendasRegistradasActivity" />
        <activity android:name=".activity.PinpadActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".receiver.LIOCancelationBroadcastReceiver">
            <intent-filter>
                <action android:name="cielo.action.NOTIFY_TRANSACTION_CANCEL">
                </action>
            </intent-filter>
        </receiver>
    </application>

</manifest>